<div class = "show-container col d-flex justify-content-center">
 <div class="card d-flex justify-content-center mb-3">
  <div class="card-body">
  <h1><%= @camera.title %></h1>

<h2>Title:</h2> <%= @camera.title %>
<h2>Description:</h2> <%= @camera.description %>
<h2>Price:</h2> <%= @camera.price %>
<h2>Owned by:</h2>
  <%= @user.first_name %>

  <div>
<div class = "camera-image ">
<%= cl_image_tag @camera.photo.key, height: 300, width: 300, crop: :fill %>
</div>

    <div data-controller="toggle" class="mt-5">
      <button class="btn btn-outline-primary m-1">
        <%= link_to "Back to Cameras", cameras_path%>
      </button>
      <%# <%= render "form", monument: @monument %>

      <button class="btn btn-outline-primary m-1">
        <%= link_to "Rent out this camera", cameras_path%>
         <%# <%= render "form", booking: @booking %>
        </button>

      <%# <%= render "form", monument: @monument %>

      <button class="btn btn-outline-primary m-1">
          <%= link_to "Edit this listing", cameras_path%>
      </button>

      <%# <%= render "form", monument: @monument %>


<div data-controller="toggle" class="mt-4">
  <button class="btn btn-outline-primary" data-action="click->toggle#fire">Book this camera</button>

  <div class = "mt-3 d-none" data-toggle-target="toggleableElement">
    <%= render "form", booking: @booking, camera: @camera %>
   </div>
</div>

<%# <%= cl_image_tag("canon_5d_mkiv_xgj7xz", width: 300, height: 300, crop: :fill) %>

    </div>
  </div>
  </div>
 </div>
</div>
<%# add image, booking, edit, "add equipment" %>
